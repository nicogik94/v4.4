# v4.4 Controlled Retrieval

This document describes the controlled-retrieval layer added after tranche 3A.

Scope implemented:

- backend-computed retrieval eligibility by phase
- whitelist-based prompt-exposure policy
- structured knowledge projection
- additive API visibility for eligible vs blocked knowledge
- bounded audit/strategy prompt assembly using eligible structured projections only
- trace visibility for which approved knowledge items audit or strategy used

Not implemented:

- broad orchestration changes
- automatic reruns or invalidation
- raw prompt dumps
- unrestricted prompt-facing retrieval
- new connectors or background sync

## Core rule

`ProjectState` remains canonical and `knowledge_layer` remains additive.

Controlled retrieval is a **derived backend view** over synced knowledge items. It does not alter:

- workflow phase semantics
- `decision_objects`
- sync behavior

## Eligibility rules

Knowledge is eligible only when all of the following are true:

- source status is `current`
- item freshness is `fresh`
- source kind / connector type are allowed for the phase
- sensitivity is allowed by phase policy
- trust tier meets the phase minimum
- prompt-exposure policy leaves at least one whitelisted field after projection

Knowledge is blocked when it is:

- stale
- expired
- quarantined
- sensitivity-disallowed
- trust-below-threshold
- source-disabled / failed / not-current
- filtered out by prompt-exposure policy

## Structured projection

Projection is whitelist-based.

Allowed output today is bounded to:

- `title`
- `summary`
- selected scalar keys from `structured_payload`

Projection does **not** expose:

- raw payload dumps
- nested payload structures
- secrets
- prompt dumps or execution transcripts

## API visibility

Additive routes:

- `GET /projects/{id}/knowledge`
- `GET /projects/{id}/knowledge/retrieval`
- `GET /projects/{id}/knowledge/retrieval/{phase}`

`/knowledge` now includes a retrieval summary.

`/knowledge/retrieval/{phase}` returns:

- phase policy
- eligible items with structured projections
- blocked items with reasons

## Audit and strategy prompt integration

In this version, only the `audit` and `strategy` phases consume controlled retrieval output.

That integration is bounded:

- eligibility is still computed in the backend
- only whitelist-projected `title`, `summary`, and approved scalar facts are included
- blocked, stale, expired, quarantined, or disallowed items are never included
- raw `structured_payload` is not dumped into prompts
- raw prompt dumps and execution transcripts are still not exposed

The audit/trace surfaces record which approved knowledge items were actually used by the audit or strategy prompt assembly.

Workspace and Trace now also show:

- whether retrieval was used for audit or strategy
- current eligible vs blocked counts
- concise blocked-reason summaries
- which approved items were actually used

These are **visibility summaries**, not causal claims. They describe current backend eligibility plus recorded usage artifacts only.

## Important boundary

Controlled retrieval is now wired into **audit and strategy only**. It is still **not** broadly integrated across phases, does not trigger reruns, and does not change orchestration semantics.
